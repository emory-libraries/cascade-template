---
id: cascade-file
label: Cascade File
message: Select a file in Cascade.
---

{{#use 'link--file' with='options'}}
{{#unless (eq options.file false)}}
<asset help-text="{{message}}"
       identifier="{{id}}"
       label="{{label}}"
       required="true"
       type="file"
       required="true"/>
{{/unless}}
{{/use}}
