---
fields:
  - id: job-title
    label: Job Title
    message: Enter the person's job title.
    required: true
---

{{#use 'bio--job'}}
{{#each fields}}
<text identifier="{{id}}"
      label="{{label}}"
      help-text="{{message}}"
      {{#if required}}required="true"{{/if}}/>
{{/each}}
{{/use}}
