---
id: image
label: Image
message:
---

{{#use 'image' with=(array 'variation' 'exclude' 'required')}}
<group identifier="{{id}}{{#if variation.id}}-{{variation.id}}{{/if}}"
       label="{{#if variation.label}}{{variation.label}} {{/if}}{{label}}">
  {{> image--file}}
  {{> image--caption}}
  {{> image--credit}}
</group>
{{/use}}
