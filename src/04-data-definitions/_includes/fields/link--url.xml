---
id: external-url
label: External URL
message: Enter an external URL. This should start with 'http://' or 'https://' (preferred).
---

{{#use 'link--url' with='options'}}
{{#unless (eq options.url false)}}
<text help-text="{{message}}"
      identifier="{{id}}"
      label="{{label}}"
      required="true"/>
{{/unless}}
{{/use}}
