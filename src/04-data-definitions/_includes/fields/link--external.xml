---
id: external-url
label: External URL
message: Enter an external URL. This should start with 'http://' or 'https://' (preferred).
---

{{#use 'link--external'}}
{{#unless (eq options.external false)}}
<text help-text="{{message}}"
      identifier="{{id}}"
      label="{{label}}"
      required="true"/>
{{/unless}}
{{/use}}
