<system-data-structure>
	{{> version}}
	{{> bio--name}}
	{{> bio--job}}
	{{> bio--location}}
	{{> image variation=(object label='Profile')}}
	{{> introduction variant='quote'}}
	{{> wysiwyg variant='biography'}}
	{{> bio--education }}
	{{> bio--contact}}
	{{> bio--subjects}}
	{{> link variation=(object id='research-guides' label='Research Guides')
					 options=(object page=false file=false url=true)
					 customizations=(object label=false description=false)}}
	{{> link variation=(object id='cv' label='Curriculum Vitae (CV)')
 					 options=(object page=false file=true url=false)
 					 customizations=(object label=false description=false)}}
</system-data-structure>
